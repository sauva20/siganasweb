# Kosongkan (biarkan default "") saat development — Vite dev server
# sudah meneruskan /api dan /public ke backend FastAPI (lihat vite.config.js).
# Isi hanya untuk build production, arahkan ke domain backend FastAPI kamu.
VITE_API_BASE_URL=
